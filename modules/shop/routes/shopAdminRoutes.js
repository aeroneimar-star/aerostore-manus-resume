"use strict";

/**
 * Rotas admin CRM para publicar/despublicar produtos.
 * Implementação completa após migrations SQL (Fase 2+ admin).
 */
function registerShopAdminRoutes() {
  // Reservado: POST /api/shop/publications, PATCH /api/shop/publications/:id
}

module.exports = {
  registerShopAdminRoutes
};
